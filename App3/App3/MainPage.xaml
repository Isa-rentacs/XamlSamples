<Page
    x:Class="App3.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:App3"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid x:Name="grid" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" SizeChanged="Grid_SizeChanged">
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>
        <Ellipse x:Name="ellipse" Grid.Row="0" Width="{Binding X}" Height="{Binding Y}" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Fill="Aqua" Loaded="ellipse_Loaded"/>
        <Ellipse x:Name="ellipse2" Grid.Row="1" Width="{Binding X}" Height="{Binding Y}" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Fill="DarkGray" Loaded="ellipse_Loaded"/>
    </Grid>
</Page>
